# To start
- `cd kubernetes`
- `kubernetes apply -f .`
- Start cert manager via command in README

# [DEPRECATED] To start docker-compose 
docker-compose up

# Notes
- Jenkins and Webserver are separated from docker-compose
-- They are to be started from their individual folders, with individual commands

