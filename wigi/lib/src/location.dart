class WigiLocation {
  String name = "name";
  String address = "address";
  double latitude = 0.0;
  double longitude = 0.0;
  List<DateTime> dates = <DateTime>[];
  String link = "link";

  WigiLocation(this.name, this.address, this.latitude, this.longitude,
      this.dates, this.link);
}
