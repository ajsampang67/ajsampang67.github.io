version: '3.3'
services:
  jenkins:
    image: jenkins/jenkins:lts
    volumes:
      - jenkins_home:/var/jenkins_home
    ports:
      - 8085:8085
      - 50000:50000
    environment:
      - JENKINS_OPTS=--httpPort=8085
      - JENKINS_ARGS=--httpKeepAliveTimeout=60000
volumes:
    jenkins_home:
